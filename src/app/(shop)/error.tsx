"use client";

import ErrorPage, { ErrorPageProps } from "@/components/ui/ErrorPage";

function ErrorPageStore(props: ErrorPageProps) {
  return <ErrorPage {...props} />;
}

export default ErrorPageStore;
