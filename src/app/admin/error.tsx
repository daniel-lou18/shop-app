"use client";

import ErrorPage, { ErrorPageProps } from "@/components/ui/ErrorPage";

function ErrorPageAdmin(props: ErrorPageProps) {
  return <ErrorPage {...props} />;
}

export default ErrorPageAdmin;
