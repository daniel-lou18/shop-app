"use client";

import ErrorPage, { ErrorPageProps } from "@/components/error/ErrorPage";

function ErrorPageGlobal(props: ErrorPageProps) {
  return <ErrorPage {...props} />;
}

export default ErrorPageGlobal;
